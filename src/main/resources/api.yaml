openapi: 3.0.3
info:
  title: Title
  description: Title
  version: 1.0.0
servers:
  - url: 'https'
paths:
  /books:
    get:
      parameters:
        - name: isbn
          in: path
          schema:
            type: string
      responses:
        200:
          description: Accepted
          content:
            application/json:
              schema:
                type: string
  
